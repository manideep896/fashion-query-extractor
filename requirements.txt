# Install via:  pip install -r requirements.txt
# setup.sh already handles all of this — use this only for manual installs.
#
# NOTE: Unsloth must be installed from git (not PyPI) for latest Qwen3 support:
#   pip install "unsloth @ git+https://github.com/unslothai/unsloth.git"

transformers>=4.43.0
trl>=0.9.0
peft>=0.12.0
accelerate>=0.33.0
bitsandbytes>=0.43.0
datasets>=2.20.0
scikit-learn>=1.5.0
numpy>=1.26.0
pandas>=2.2.0
tqdm>=4.66.0
outlines>=0.0.46
huggingface_hub>=0.24.0
